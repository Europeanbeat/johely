{
  "name": "johely-redesign",
  "private": true,
  "scripts": {
    "build": "npm --prefix islands run build && node scripts/build-site.mjs",
    "preview": "python3 -m http.server 4187 --bind 127.0.0.1 --directory dist",
    "check": "node scripts/check-site.mjs"
  }
}
